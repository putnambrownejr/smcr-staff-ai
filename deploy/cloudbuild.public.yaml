# Invoke with an explicit project and _IMAGE substitution; uploads use the allowlist.
steps:
  - name: gcr.io/cloud-builders/docker
    args: [build, -f, Dockerfile.public, -t, '${_IMAGE}', .]
images:
  - '${_IMAGE}'
